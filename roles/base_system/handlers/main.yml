---
- name: Applica la configurazione netplan
  ansible.builtin.command:
    cmd: netplan apply
  changed_when: true

- name: Riavvia systemd-resolved
  ansible.builtin.systemd_service:
    name: systemd-resolved
    state: restarted
